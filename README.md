# Hentai-IMG 多模式爬虫（工程级版本）

本项目是一个支持多模式、多来源、跨关键词复用目录、自动跳过已完成作品的工程级爬虫框架。

支持的列表模式：

- `search_keyword`
- `search_tag`
- `ranking`
- `ranking_video`
- `ranking_download`
- `ranking_bookmark`
- `ranking_like`
- `search_video`

支持的作品类型：

- 多图作品（Story Viewer）
- 单图作品
- 视频作品（mp4 / m3u8）

---

## 📁 项目结构

your_project/
│
├─ config.py           # 全局配置、日志系统、哈希目录方案
├─ utils.py            # 工具函数（meta.json、跳过逻辑、目录查找）
├─ parser_list.py     # 列表页解析（一般 + 特殊）
├─ parser_story.py    # Story Viewer 解析（多图 / 单图 / 视频）
├─ downloader.py       # 图片 / 视频下载
├─ main.py             # 主流程 crawl()
│
├─ download/          # 自动生成的作品目录
├─ logs/              # 自动生成的日志文件
└─ 备份/              # 我备份的一些文件

## 🧩 目录命名规则（哈希方案）

### 一般情况（有日期）

{date}丨{title_truncated}丨{total}丨{hash}
### 特殊情况（无日期）

{hash}丨{title_truncated}丨{total}
- `title_truncated`：标题前 100 字符
- `hash`：slug 的 md5 前 8 位
- `slug`：详情页 URL 的最后一段

---

## 📝 meta.json 字段说明

每个作品目录下会自动生成 `meta.json`：

```json
{
  "slug": "...",
  "hash": "...",
  "title": "完整标题",
  "title_truncated": "截断标题",
  "date": "2026-01-29",
  "total": 296,
  "file_type": "image / video",
  "thumb_url": "缩略图 URL",
  "list_source": "search_keyword:stripping",
  "download_time": "2026-01-30 18:05:22",
  "source": "详情页 URL"
}
```

## ▶️ 运行方式Windows

双击：

```cmd
run.bat
```

或：

```cmd
python main.py
```

Linux / macOS

```cmd
chmod +x run.sh
./run.sh
```

## 📦 安装依赖

```cmd
pip install -r requirements.txt
```

## 🧠 特殊情况说明

当列表页没有：

搜索结果总数

最后一页链接（.wp-pagenavi a.last）

爬虫会自动进入 特殊模式：

从当前页开始

通过 nextpostslink 一直往后爬

直到没有下一页

## 🛠️ 断点续爬

爬虫会自动跳过：

已完成文件

已记录的无效链接

已存在的作品目录（跨关键词复用）

无需手动处理。

## 📜 日志系统

```cmd
logs/crawler_YYYYMMDD_HHMMSS.log
```

